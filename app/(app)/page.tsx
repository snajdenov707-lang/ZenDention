// Захват — главный экран. Пока пустой, содержимое в следующей итерации.
export default function CapturePage() {
  return (
    <section>
      <h1 className="text-[22px]">Захват</h1>
      <p className="mt-2 text-[14px] text-[var(--color-fg-muted)]">
        Голос сюда. Всё остальное — сама.
      </p>
    </section>
  );
}
